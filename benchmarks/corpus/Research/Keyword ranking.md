# Keyword ranking

BM25 rewards rare query terms, repeated evidence, and reasonable document length. Title and phrase boosts make obvious matches visible. It cannot reliably connect synonyms, so a second semantic signal can improve recall.
