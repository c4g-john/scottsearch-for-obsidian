# Vector retrieval

Embedding nearby meanings into neighboring numeric positions lets a query retrieve a passage that uses different vocabulary. Similarity is useful for concept discovery, but lexical evidence and hard filters keep the ranking grounded.
