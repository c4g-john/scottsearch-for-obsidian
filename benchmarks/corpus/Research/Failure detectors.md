# Failure detectors

Timeouts provide imperfect evidence that a process has crashed or the network is delayed. Distributed systems combine suspicion, heartbeats, and quorum rules so liveness can recover without treating every slow machine as permanently lost.
